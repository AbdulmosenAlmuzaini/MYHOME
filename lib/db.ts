import { 
  queryDatabase, 
  ensureDatabaseTables, 
  getApprovedSubmissions, 
  getAllSubmissions, 
  insertSubmission, 
  updateSubmissionStatus 
} from '@/DATABASES/db';

export { 
  queryDatabase, 
  ensureDatabaseTables, 
  getApprovedSubmissions, 
  getAllSubmissions, 
  insertSubmission, 
  updateSubmissionStatus 
};
